function sum(a,b){
    return a + b;
}

// Ejercicio 1
function pin(pin) {
    if (pin ==! null && Number.parseInt(pin) && (String.length(pin) >= 4 && String.length(pin) <= 6)) {

    }
}


module.exports = {sum};